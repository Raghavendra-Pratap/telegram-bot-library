# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here

# User Access Control
# ENABLE_USER_VERIFICATION=true  → new users must be approved before they can use the bot
# ALLOWED_USER_IDS               → pre-approved user IDs (comma-separated), bypass approval
# ADMIN_USER_ID                  → the Telegram user ID that receives approval requests
#                                   Required when ENABLE_USER_VERIFICATION=true
# Example: ALLOWED_USER_IDS=123456789,987654321
# To get your user ID, message @userinfobot on Telegram
ENABLE_USER_VERIFICATION=false
ALLOWED_USER_IDS=
ADMIN_USER_ID=

# Optional: Instagram Credentials (for Reels/Stories)
INSTAGRAM_USERNAME=
INSTAGRAM_PASSWORD=

# Optional: YouTube Cookies (for private videos)
# Format: cookies.txt file path or leave empty
YOUTUBE_COOKIES_PATH=

# Download Settings
DOWNLOAD_DIR=./downloads
MAX_VIDEO_QUALITY=2160p
PREFERRED_QUALITY=best

# Local File Server (for download links)
ENABLE_FILE_SERVER=true
FILE_SERVER_PORT=8080
FILE_SERVER_HOST=localhost

# Playlist upload concurrency (default 2 — safe under Telegram flood limits)
MAX_CONCURRENT_UPLOADS=2

# Background worker pools (instant user replies; work runs in queues)
ANALYZE_WORKERS=3
DOWNLOAD_WORKERS=3
UPLOAD_WORKERS=2

# Max size (MB) the bot may upload per file via Telegram cloud API (default 50).
# Telegram Premium does not raise this for bots; use a local Bot API server for larger files.
MAX_BOT_UPLOAD_MB=50

