# Telegram Bot API Token
# Get from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# MTProto API Credentials (for premium account)
# Get from https://my.telegram.org
TELEGRAM_API_ID=your_api_id_here
TELEGRAM_API_HASH=your_api_hash_here
# Session name - just a filename for storing your login session
# You can leave this as default or change it to any name you like
# This creates a file like "premium_account.session" in the bot directory
TELEGRAM_SESSION_NAME=premium_account

# User Access Control
# Set to "true" to enable user verification
ENABLE_USER_VERIFICATION=false

# Dynamic User Management (Recommended)
# Set to "true" to use dynamic user management (add/remove users via commands)
# Set to "false" to use hardcoded user IDs from ALLOWED_USER_IDS
USE_DYNAMIC_USER_MANAGEMENT=true
# File to store allowed users (JSON format)
USERS_FILE=allowed_users.json

# Initial Admin User IDs (Optional - for setting admins from .env)
# Comma-separated list of user IDs that should be admins on first run
# Example: INITIAL_ADMIN_USER_IDS=123456789,987654321
# If not set, first user to use /adduser becomes admin
# Get your user ID: Send /start to @getidsbot on Telegram
INITIAL_ADMIN_USER_IDS=

# Legacy: Hardcoded user IDs (only used if USE_DYNAMIC_USER_MANAGEMENT=false)
# Comma-separated list of allowed user IDs
ALLOWED_USER_IDS=

# Download Settings
DOWNLOAD_DIR=./downloads

# File Server Settings (for direct downloads)
# Set to "true" to enable HTTP file server
ENABLE_FILE_SERVER=true
FILE_SERVER_HOST=0.0.0.0
FILE_SERVER_PORT=8080
# Base URL for download links
# 
# For LOCAL development (testing on same computer):
#   FILE_SERVER_BASE_URL=http://localhost:8080
#
# For LOCAL network access (from phone/other devices on same WiFi):
#   1. Find your local IP: 
#      - Mac/Linux: run "ifconfig" or "ip addr" and look for your WiFi IP (usually 192.168.x.x)
#      - Windows: run "ipconfig" and look for IPv4 Address
#   2. Use: FILE_SERVER_BASE_URL=http://YOUR_LOCAL_IP:8080
#      Example: FILE_SERVER_BASE_URL=http://192.168.1.100:8080
#
# For PRODUCTION (public server):
#   FILE_SERVER_BASE_URL=https://yourdomain.com
#   or
#   FILE_SERVER_BASE_URL=http://your.server.ip:8080
#
FILE_SERVER_BASE_URL=http://localhost:8080

# File Retention (in hours)
FILE_RETENTION_HOURS=24

# Logging
LOG_LEVEL=INFO
